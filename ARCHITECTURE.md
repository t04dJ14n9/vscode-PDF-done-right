# PaperLink Architecture — Visual Reference

## 1. System Architecture

```
┌──────────────────────────────────────────────────────────────────┐
│                     VS CODE EDITOR                               │
│  ┌─────────────────────┐              ┌─────────────────────┐   │
│  │  Markdown Editor    │              │   PDF Viewer Tab    │   │
│  │                     │              │  (Webview iframe)   │   │
│  │ @pdf[[paper.pdf#... │              │                     │   │
│  │ p.5|"text"]]        │              │ PDFium Rendering    │   │
│  └──────────┬──────────┘              └────────────┬────────┘   │
│             │                                      │             │
└─────────────┼──────────────────────────────────────┼─────────────┘
              │                                      │
    ┌─────────▼──────────┐                ┌─────────▼──────────┐
    │ PdfLinkProvider    │                │ PdfEditorProvider  │
    │ (DocumentLinks)    │                │ (WebviewPanel)     │
    └─────────┬──────────┘                └─────────┬──────────┘
              │                                     │
              └──────────────┬────────────────────┬─┘
                             │                    │
                    ┌────────▼────────┐           │
                    │ extension.ts    │           │
                    │ (Hub)           │           │
                    │                 │           │
                    │ activate()      │◄──────────┘
                    │ -> register all │
                    │    components   │
                    └────────┬────────┘
                             │
                ┌────────────┼────────────┐
                │            │            │
        ┌───────▼──────┐    │   ┌────────▼────────┐
        │AnnotationSvc │    │   │PdfOutlineProvider
        │ (Sidecar)    │    │   │ (TreeView)
        │ JSON storage │    │   │
        └──────────────┘    │   └────────┬────────┘
                            │            │
                            │      Sidebar Explorer
                ┌───────────▼─────────────┘
                │
        ┌───────▼────────────────┐
        │ Markdown-it Plugin     │
        │ (Preview rendering)    │
        └────────────────────────┘
```

## 2. Data Flow Diagram

### A. Forward Direction (Markdown → PDF)

```
┌─────────────────┐
│ Markdown Editor │
│ with PDF links  │
└────────┬────────┘
         │ User clicks
         │ @pdf[[papers/paper.pdf#...]]
         ▼
┌─────────────────────────────┐
│ PdfLinkProvider             │
│ provideDocumentLinks()      │
│ - Find pattern with regex   │
│ - Create DocumentLink       │
│ - Return to VS Code        │
└────────┬────────────────────┘
         │ VS Code renders
         │ underlined link
         ▼
    [CLICKABLE LINK]
         │ User clicks
         ▼
┌─────────────────────────────────────┐
│ VS Code Command URI Handler         │
│ Executes:                           │
│ paperlink.openPdfAtAnchor           │
└────────┬────────────────────────────┘
         │ Command + args
         ▼
┌─────────────────────────────────────┐
│ Extension Host                      │
│ PdfEditorProvider                   │
│ .openPdfAtAnchor(path, anchor)      │
│ 1. Open PDF with custom editor      │
│ 2. Wait for webview                 │
│ 3. Send goToAnchor message          │
└────────┬────────────────────────────┘
         │ postMessage
         │ { type: 'goToAnchor',
         │   anchor: {...} }
         ▼
┌─────────────────────────────────────┐
│ Webview (pdf-viewer.ts)             │
│ PdfViewer.goToAnchor()              │
│ 1. Scroll to page                   │
│ 2. Highlight temporary (2s)         │
│ 3. Update page info                 │
└─────────────────────────────────────┘
```

### B. Backward Direction (PDF → Markdown)

```
┌─────────────────┐
│  PDF Viewer     │
│ (Webview)       │
└────────┬────────┘
         │ User selects text
         │ (mouseup event)
         ▼
┌──────────────────────────────────┐
│ PdfViewer.handleTextSelection()   │
│ 1. Get selected text              │
│ 2. Find text layer element        │
│ 3. Map to text item indices       │
│ 4. Create PdfAnchor               │
└──────────────┬───────────────────┘
               │ selectionToAnchor()
               │ returns PdfAnchor
               ▼
┌───────────────────────────────────┐
│ PdfViewer.showSelectionToolbar()  │
│ - "Copy Link" button              │
│ - "Insert in Note" button         │
└──────────┬────────────────────────┘
           │ User clicks
           │ "Copy Link"
           ▼
┌─────────────────────────────────────┐
│ Webview sends message:              │
│ { type: 'copyLinkToClipboard',      │
│   anchor: PdfAnchor }               │
└────────┬────────────────────────────┘
         │ postMessage
         ▼
┌─────────────────────────────────────┐
│ Extension Host (case handler)       │
│ 1. Get relative PDF path            │
│ 2. formatPdfLink()                  │
│ 3. Copy to clipboard                │
│ 4. Show success message             │
└─────────────────────────────────────┘
         │
         └──► Clipboard: @pdf[[...#...]]
         
         User pastes in markdown file
```

### C. Annotation Display Flow

```
┌─────────────────┐
│ PDF opens in    │
│ custom editor   │
└────────┬────────┘
         │ onDidChangeViewState
         │ (active PDF)
         ▼
┌──────────────────────────────────────┐
│ PdfEditorProvider                    │
│ loadPdfIntoWebview(webview, pdfUri)  │
│ 1. Read PDF file (base64)            │
│ 2. Send to webview: loadPdf          │
│ 3. Call sendAnnotations()            │
└──────────────────┬───────────────────┘
                   │ 
         ┌─────────▼────────────────┐
         │ AnnotationService        │
         │ getAnnotationsForPdf()   │
         │ - Check cache            │
         │ - Read .paperlink.json   │
         │ - Return Annotation[]    │
         └─────────┬────────────────┘
                   │ postMessage
                   │ { type: 'highlightAnnotations',
                   │   annotations: [...] }
                   ▼
┌──────────────────────────────────────┐
│ Webview (pdf-viewer.ts)              │
│ PdfViewer.annotations = msg.annot    │
│ redrawAllHighlights()                │
└──────────────┬───────────────────────┘
               │ drawHighlightsForPage()
               │ for each page:
               ▼
┌──────────────────────────────────────┐
│ Create colored overlay divs          │
│ Position over text items             │
│ Add click listeners                  │
└──────────────┬───────────────────────┘
               │ User clicks highlight
               ▼
┌──────────────────────────────────────┐
│ Webview sends:                       │
│ { type: 'annotationClicked',         │
│   annotationId: '...' }              │
└──────────────┬───────────────────────┘
               │ postMessage
               ▼
┌──────────────────────────────────────┐
│ Extension Host                       │
│ 1. Find annotation by ID             │
│ 2. Get markdown file path            │
│ 3. Get blockRef                      │
│ 4. openMarkdownAtRef()               │
└──────────────┬───────────────────────┘
               │ Opens in editor
               │ + scrolls to block
               ▼
    Markdown file visible with
    annotation context highlighted
```

## 3. Component Interaction Diagram

```
                        ┌──────────────────────────┐
                        │  VS CODE CORE            │
                        │  - Editor manager        │
                        │  - Command palette       │
                        │  - Workspace             │
                        └──────┬───────────────────┘
                               │
                    ┌──────────┴──────────┐
                    │                     │
        ┌───────────▼──────────┐  ┌──────▼────────────┐
        │ Markdown Editor      │  │ Custom Editor     │
        │ - Doc Link Provider  │  │ - PDF Viewer      │
        │ - Markdown-it Plugin │  │ - Selection       │
        └───────────┬──────────┘  └────────┬──────────┘
                    │                      │
                    └──────────┬───────────┘
                               │
                    ┌──────────▼──────────┐
                    │ extension.ts        │
                    │ Activation Hub      │
                    └──────────┬──────────┘
                               │
                ┌──────────────┼──────────────┐
                │              │              │
        ┌───────▼────┐  ┌──────▼──────┐  ┌──▼──────────┐
        │ PDF Link   │  │ Annotation  │  │ PDF Outline│
        │ Provider   │  │ Service     │  │ Provider   │
        └───────┬────┘  └──────┬──────┘  └──┬─────────┘
                │              │            │
                │              ▼            │
                │        ┌─────────────┐    │
                │        │ Sidecar JSON│    │
                │        │ .pdf.paper  │    │
                │        │ link.json   │    │
                │        └─────────────┘    │
                │              │            │
                └──────────┬───┴────────────┘
                           │
                    ┌──────▼───────┐
                    │ Shared Types │
                    │ src/shared/  │
                    │ types.ts     │
                    └──────┬───────┘
                           │
                ┌──────────┴──────────┐
                │                     │
        ┌───────▼────────┐  ┌────────▼────────┐
        │ Extension Code │  │ Webview Code    │
        │ Node.js        │  │ Browser/WASM    │
        │ - Coordination │  │ - Rendering     │
        │ - File I/O     │  │ - Selection     │
        └────────────────┘  │ - Text Layout   │
                            └─────────────────┘
```

## 4. Message Queue Architecture

```
┌────────────────────────────┐         ┌──────────────────────────┐
│   Extension Host           │         │   Webview (pdf-viewer)   │
│   (src/*.ts)               │         │   (webview-src/*.ts)     │
│                            │         │                          │
│  window.postMessage() ──────────────► window.addEventListener() │
│  (sends message queue)     │         │   (receives)             │
│                            │         │                          │
│ ◄───────────────────────────────────── window.postMessage()     │
│ (receives)                 │         │   (sends)                │
│                            │         │                          │
└────────────────────────────┘         └──────────────────────────┘

Message Types (Extension → Webview):
┌─────────────────────────────────────┐
│ loadPdf: { data: base64_string }    │
│ goToAnchor: { anchor: PdfAnchor }   │
│ highlightAnnotations: {...}         │
│ setTheme: { theme: 'light'|'dark' } │
└─────────────────────────────────────┘

Message Types (Webview → Extension):
┌─────────────────────────────────────────┐
│ ready: {}                               │
│ copyLinkToClipboard: { anchor: ... }   │
│ requestInsertLink: { anchor: ... }     │
│ annotationClicked: { annotationId }    │
│ outline: { items: PdfOutlineItem[] }   │
│ pageChanged: { page: number }          │
│ selectionMade: { anchor: ... }         │
└─────────────────────────────────────────┘
```

## 5. Rendering Pipeline

```
PDF File (Binary)
     │
     ▼
┌─────────────────────────────────┐
│ PdfEditorProvider               │
│ loadPdfIntoWebview()            │
│ - Read file                     │
│ - Base64 encode                 │
│ - postMessage('loadPdf')        │
└──────────┬──────────────────────┘
           │ base64 string
           ▼
┌─────────────────────────────────┐
│ Webview: PdfViewer              │
│ loadPdf(base64Data)             │
│ - Decode bytes                  │
│ - engine.openDocumentBuffer()   │
│ - Create page DOM structure     │
│ - renderAllVisiblePages()       │
└──────────────┬──────────────────┘
               │
        ┌──────▼──────────┐
        │ IntersectionObs │
        │ observer        │
        └──────┬──────────┘
               │ triggers on visibility
               ▼
┌─────────────────────────────────┐
│ renderPage(pageNum)             │
│ 1. engine.renderPage() → Blob   │
│ 2. Draw to canvas               │
│ 3. engine.getPageTextRects()    │
│ 4. Build text layer (spans)     │
│ 5. drawHighlightsForPage()      │
└──────────────┬──────────────────┘
               │
        ┌──────┴──────────────────┐
        │                         │
    Canvas              Text Layer
  (pixels)          (invisible spans)
                          │
                  Selection & 
                  Highlighting
```

## 6. State Machine: PDF Viewer

```
                    ┌─────────────────┐
                    │  Initial State  │
                    │  (no PDF)       │
                    └────────┬────────┘
                             │
                    loadPdf() │
                             ▼
            ┌──────────────────────────────┐
            │  Loading State               │
            │ - engine.openDocumentBuffer()│
            │ - renderAllVisiblePages()    │
            └──────────────────────────────┘
                             │
                    ready    │
                             ▼
            ┌──────────────────────────────┐
            │  Loaded State                │
            │ - pages rendered (lazy)      │
            │ - text layers built          │
            │ - annotations shown          │
            └──────────────────────────────┘
                             │
                    ┌────────┼────────┐
                    │        │        │
              zoom()│   select │goTo   │
                    │    text │page() │
                    ▼        ▼        ▼
        ┌──────┐ ┌──────┐ ┌────────┐
        │Zoom  │ │Select│ │Navigate│
        │State │ │State │ │State   │
        └──────┘ └──────┘ └────────┘
                    │        │        │
                    └────────┼────────┘
                             │
                    back to Loaded State
```

## 7. File Storage & Persistence

```
Workspace Root
│
├── papers/
│   ├── attention.pdf
│   └── attention.pdf.paperlink.json  ◄─── Sidecar
│       {
│         "version": 1,
│         "pdfFile": "attention.pdf",
│         "annotations": [
│           {
│             "id": "uuid",
│             "anchor": {...},
│             "markdownFile": "notes/research.md",
│             "blockRef": "^section-1",
│             "color": "rgba(...)",
│             "createdAt": "2024-04-17..."
│           }
│         ]
│       }
│
├── notes/
│   ├── research.md
│   │   Content includes:
│   │   @pdf[[../papers/attention.pdf#page=5&idx=12&off=5&len=40|"snippet"]]
│   │
│   └── other.md
│
└── ...other files
```

## 8. TypeScript Compilation Flow

```
Source Files
├── src/**/*.ts (Node.js target)
├── webview-src/**/*.ts (Browser target)
└── src/shared/types.ts (Both targets)
        │
        ▼
    Webpack Config
    (3 entry points)
        │
    ┌───┴────────────────┐
    │                    │
    ▼                    ▼
   ts-loader (main)   ts-loader (webview)
   + tsconfig.json    + tsconfig.webview.json
    │                    │
    ├───────┬────────────┤
    │       │            │
    ▼       ▼            ▼
dist/      dist/         dist/
extension. pdf-viewer.   markdown-
js         js            preview.js
            │
        ┌───┴───────────────────┐
        │ CopyPlugin            │
        │ (pdfium.wasm)         │
        └───┴───────────────────┘
            │
            ▼
        dist/pdfium.wasm
```

## 9. Search & Discovery Flow

```
User in Markdown Editor
        │
        ▼
Searches for PDF references
"Which PDFs link to this note?"
        │
        ▼
AnnotationService
.findAnnotationsForMarkdown(mdPath)
        │
        ├─ vscode.workspace
        │  .findFiles("**/*.paperlink.json")
        │
        ├─ For each sidecar file:
        │  ├─ Read & parse JSON
        │  ├─ Find annotations
        │  │  matching mdPath
        │  └─ Collect results
        │
        ▼
Results: [
  { pdfUri, annotation },
  { pdfUri, annotation },
  ...
]
        │
        ▼
Display to user
"Open PDF at page..."
```

## 10. Markdown Preview Integration

```
Markdown File
@pdf[[papers/paper.pdf#page=5&idx=12&off=5&len=40|"text"]]
        │
        ▼
markdown-it Plugin
activateMarkdownItPlugin()
        │
        ├─ Intercept text rendering
        ├─ Check against PDF_LINK_REGEX
        ├─ Replace with styled <a> tag
        │
        └─► <a class="paperlink-pdf-link"
               href="#"
               data-pdf-path="papers/paper.pdf"
               data-pdf-anchor="page=5&idx=12&off=5&len=40"
               style="...blue styling...">
             📄 papers/paper.pdf p.5
            </a>
        │
        ▼
VS Code Markdown Preview (Webview)
        │
        ├─ markdown-preview.ts (injected)
        ├─ Click on .paperlink-pdf-link
        ├─ Extract data-* attributes
        ├─ Create command URI
        │
        ▼
Command URI:
command:paperlink.openPdfAtAnchor?
  {"pdfPath":"papers/paper.pdf","anchor":"page=5&idx=12&off=5&len=40"}
        │
        ▼
VS Code Command Handler
        │
        ▼
Extension Host
paperlink.openPdfAtAnchor command
(Same as markdown editor link flow)
```

---

## Quick Reference: Module Responsibilities

| Module | Responsibility | I/O |
|--------|----------------|-----|
| `extension.ts` | Activation & registration | Event handlers |
| `PdfEditorProvider` | Webview lifecycle & messaging | Files ↔ Webview |
| `AnnotationService` | Persistence & caching | Sidecar JSON ↔ Memory |
| `PdfLinkProvider` | Link detection in markdown | Document text → Links |
| `PdfOutlineProvider` | Outline tree display | Bookmarks → Tree items |
| `markdownPlugin.ts` | HTML generation for preview | Text → HTML |
| `PdfViewer` | PDF rendering & interaction | PDF data ↔ DOM |
| `markdown-preview.ts` | Link handling in preview | Click → Command |
| `types.ts` | Type definitions & serialization | Host ↔ Webview contract |

---

